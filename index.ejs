<!DOCTYPE html>
<html>

<head>
    <meta charset="utf-8" />
    <meta content="ie=edge" http-equiv="x-ua-compatible" />
    <title>
        <%= htmlWebpackPlugin.options.title %>
    </title>
</head>

<body>
    <!-- 
        <button id="button_export">Export</button>
        note might need local token, maybe not?
    controller=1 is required

    #editor not needed
    -->
    <iframe id="editor" src="<%= htmlWebpackPlugin.options.domain %>/index.html?controller=1" style="
                margin: 0;
                border: none;
                width: 100%;
                height: 100%;
                position: absolute;
                left: 40;
                top: 0;
            ">
    </iframe>
</body>

</html>